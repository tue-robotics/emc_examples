Terminal 1 -- build the project:
mkdir build
cd build
cmake ..
make

Terminal 2 -- start a roscore:
roscore

Terminal 3 -- start the simulator:
emc-sim

Terminal 4 -- visualize the sensor data (optional)
emc-viz

Terminal 1 -- run the program:
../bin/main
